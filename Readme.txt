
< AI 모델 개선 순서 >

1. 유량 예측 모델 학습
-> 학습 데이터의 경로를 data_flow로 변경

2. 학습된 모델로 예측을 수행해 본다.
-> 예측 결과 : 유량

3. 예측된 유량과 실제 유량 (엑셀에서 읽어온)을 비교해보는 그래프를 그린다.

4. 예측된 유량과 실제 유량의 평가 지표를 계산해본다.

5. 예측 유량을 수위로 변환
-> QHCurve(flow) 사용

6. 변환된 수위와 실제 수위 (엑셀에서 읽어온) 을 비교해보는 그래프를 그린다.
-> drawCorrectionGraph(output, y_pred, 'waterlevel')

7. 변환된 수위와 실제 수위의 평가 지표를 계산한다.

-------------------------------------------------

0. 최종 목표 : 유량 예측모델

36step의 관측 강우/수위와 72step의 예보강우로 수위 예측을 하는 AI 모델이다. (1step = 10분)
-> 6시간까지만 예측하는 모델로 구성 되도록 레이어 뒷부분을 자르고 싶어.
-> 파라미터 조정 + 코드 수정 

-------------------------------------------------------

< AI 모델 학습데이터 보강 및 성능개선 순서 >

1. 자료수집
- 2014 ~ 2025년까지의 관측소 자료 수집 (강우, 유량)
- csv로 저장 ( xlsx <= 파일이 너무 무거워짐 )

조회 : 홍수통제소 데이터 수집 범위가 한정되어있음 (1개월단위)
예 : for (~~ range(10)) 월별 자료조회 코드


저장 : 1년단위로 저장
예 : 2014_dataset.csv 
파일 컬럼 형식 : time, 서울시(대곡교), ....., 대곡교_수위, 대곡교_유량, 궁내교_수위, 궁내교_유량

=> 결과 : 연도별 10분단위 강우 / 수위 / 유량 관측자료

2. 연도별로 비가 왔었던 구간을 추출 후 ,
   관측소 수위가 일정 단계 이상 도달한 경우 다시 추출 (관심단계 이상)
- MIET (강우 이벤트 분리) 수정
- IETD (관심수위 이상 이벤트 추출) 수정

=> 결과 : 연도별로 분리된 강우 / 수위 / 유량 학습자료

2.1 학습데이터 증강
2.2 학습데이터 그래프 만들기
2.3 원래 학습자료 구조 변경

3. Pararell LSTM에 분리된 강우 사상을 추가해서 학습 시도

3.1 Pararell LSTM 학습 , 예측 모델 수정

4. 학습 완료후 결과 평가 (R2, mse, mae, rmse)

-------------------------------------------------------

5-1. 모델 개발자한테 자문 요청
5-2. 우리가 LSTM 개발
5-3. Transformer 모델로 개발 <= 난이도 알수없음